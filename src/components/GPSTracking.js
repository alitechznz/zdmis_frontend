import React from 'react';

const GPSTracking = () => (
  <div className="container mt-4">
    <h2>GPS Vessel Tracking</h2>
    <p>Track vessels in real-time using GPS.</p>
  </div>
);

export default GPSTracking;
